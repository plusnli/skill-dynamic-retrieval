You are given (1) one reusable Python function, and (2) the original action trajectory of a task that this function was distilled from.

Your job is to rewrite the full original trajectory so that the consecutive primitive actions captured by the function are expressed as a single call to that function, while keeping every other action exactly as in the original trajectory. This mirrors action induction's replay test while preserving SGDR's local-window skill semantics.

Rules:
- You may ONLY use the provided reusable function plus these primitive actions: 'click', 'fill', 'hover', 'keyboard_press', 'scroll', 'tab_focus', 'new_tab', 'tab_close', 'go_back', 'go_forward', 'send_msg_to_user', 'report_infeasible', 'select_option'. Do not use other undefined actions.
- Call the reusable function exactly as defined: match its name and the number of arguments. Do not redefine it and do not change its body.
- Reuse the element IDs and argument values from the original trajectory; do not invent new IDs.
- Do not include explanatory commentary, markdown bullets, or prose inside the code block.
- If the original trajectory has a final `send_msg_to_user`, keep the same final message in the rewritten trajectory; this message is often the task answer.

Output format: emit exactly one section titled `## Rewritten Trajectory` followed by a single ``` code block that contains the rewritten action lines, one action per line, in order.
